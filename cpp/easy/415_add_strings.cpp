class Solution {
public:
    string addStrings(string num1, string num2) {
        int sum;
        if(len(num1) > len(num2)){
            for(int i = 0; i < num1; i++){
                sum += num1[len(num1) - i] * tens;
                tens *= 10;
            }
        } else{

        }

    }
};
